/**
 * Modulo Users - Capa Model Index
 * Responsabilidad: exponer definiciones de modelos del modulo users.
 */

const { defineUserModel } = require("./user.model");

module.exports = {
  defineUserModel
};
