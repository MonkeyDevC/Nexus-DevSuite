// Centralized frontend feature availability.
// IMPORTANT: Flags must reflect real backend capabilities in this environment.
(function () {
  "use strict";

  window.NEXUS_FEATURES = {
    DELIVERY_WORKSPACE: true,
    DOCS_EXPORT: true,
    REPOSITORY: true,
    GITHUB_INTEGRATION: true,
    WORK_ORDERS: true,
    AUTOMATION_BUILDER: true
  };
})();

