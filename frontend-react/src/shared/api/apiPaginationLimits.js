/**
 * Límites de paginación alineados con validadores backend (p. ej. `backlog.validator` para stories por proyecto).
 * El frontend no debe solicitar `limit` por encima de estos valores.
 */
export const API_MAX_PROJECT_STORIES_PAGE_SIZE = 100;
