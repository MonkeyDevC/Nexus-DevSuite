/**
 * Entrada router: Product Backlog modular. Ruta canónica `/backlog` (+ `?project=`). Legacy `/projects/:id/backlog` redirige.
 */
export { default } from "../modules/backlog/product-backlog/ProductBacklogPage.jsx";
