/**
 * Ruta de detalle de proyecto: delega en ProjectDetailWorkspace (portal, tabs, borrador, stack feature/story).
 */
import { ProjectDetailWorkspace } from "../modules/projects/index.js";

export default function ProjectDetail() {
  return <ProjectDetailWorkspace />;
}
